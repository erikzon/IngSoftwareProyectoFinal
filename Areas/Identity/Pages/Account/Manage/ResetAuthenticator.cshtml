@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Restablecer clave de autenticación";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Si restableces tu clave de autenticador, tu aplicación de autenticador no funcionará hasta que la vuelvas a configurar.</strong>
    </p>
    <p>
        Este proceso desactiva la autenticación de dos factores (2FA) hasta que verifiques tu aplicación de autenticador.
        Si no completas la configuración de tu aplicación de autenticador, puedes perder el acceso a tu cuenta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Restablecer clave de autenticador</button>
    </form>
</div>
